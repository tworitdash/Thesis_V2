A = load('SPP_conv.mat');

F = 4e9:0.5e9:21e9;

R = 0.0405319403216/2.1;

f =  fc(R, 1, 1);
    

for i = 1:30
    freq = f(i) + eps;
%     j = find(abs(F - freq) * 1e-9 < 0.1);
%     if j == []
        j = find(m(abs(F - freq)));
%     end
    Spp(i) = A.Spp(i).Spp_i(j);
end

